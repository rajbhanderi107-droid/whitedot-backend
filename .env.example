# ── Postgres (self-hosted in Docker on VPS 187.127.185.57) ──────────────────
# db hostname = "db" (Docker service name inside compose network)
DATABASE_URL="postgresql://whitedot:CHANGE_ME@db:5432/whitedot"
DIRECT_URL="postgresql://whitedot:CHANGE_ME@db:5432/whitedot"
POSTGRES_PASSWORD="CHANGE_ME"

# ── Auth ─────────────────────────────────────────────────────────────────────
# Generate: openssl rand -hex 32
JWT_SECRET="CHANGE_ME"

# ── Server ───────────────────────────────────────────────────────────────────
PORT=4000
NODE_ENV=production
FRONTEND_URL="https://whitedotindia.in"
FRONTEND_ORIGINS="https://whitedotindia.in"

# ── Seed admin ───────────────────────────────────────────────────────────────
ADMIN_SEED_EMAIL="admin@whitedot.in"
ADMIN_SEED_PASSWORD="CHANGE_ME"

# ── Google OAuth ─────────────────────────────────────────────────────────────
GOOGLE_CLIENT_ID="689571813571-q5ctmgkmu8vt1cvgbeeubqdmabs2a7ec.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="CHANGE_ME"

# ── AI agents ────────────────────────────────────────────────────────────────
GOOGLE_AI_KEY="CHANGE_ME"
ANTHROPIC_API_KEY="CHANGE_ME"
